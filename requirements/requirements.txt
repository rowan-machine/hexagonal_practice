# Core dependencies
# Note: pandas is optional - the system works without it but with limited DataFrame operations

# Required for YAML configuration
pyyaml>=6.0

# Type checking (optional, for development)
# mypy>=1.0.0
# types-python-dateutil>=2.8.0

# Testing (optional, for development)
# pytest>=7.0.0
# pytest-cov>=4.0.0

# Data processing (required for DataFrame operations and notebooks)
pandas>=2.0.0

# HTTP client for Atlas integration
requests>=2.31.0

# PostgreSQL support (optional, for production)
# psycopg2-binary>=2.9.0  # Uncomment if using PostgreSQL

# Note: This is a minimal requirements file.
# The system is designed to work with standard library only for core functionality.
# Add pandas if you need DataFrame operations in the transform layer.

-r requirements-dev.txt
